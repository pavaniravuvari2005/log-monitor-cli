# Log Monitor CLI Tool

A Python-based command-line tool to analyze system log files and generate summary reports.

## Features
- Count log levels (INFO, WARNING, ERROR)
- Filter logs by severity
- Simple CLI interface

## Usage
```bash
python main.py logs/sample.log
python main.py logs/sample.log --filter ERROR
